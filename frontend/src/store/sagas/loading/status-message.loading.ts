import type { TLoadingStatus } from '../../slices/slices.type';

function* loadingStatusWorker(action: { payload: { status: TLoadingStatus }; type: string }) {
  //
}

function* loadingStatusWatcher() {
  //
}

export { loadingStatusWatcher };
